// Component = Function that returns HTML (JSX)
// All components must start with a capital letter
// Attribute in HTML = Property in React

import { Widget } from "./components/Widget";

export function App() {
  return <Widget />;
}
